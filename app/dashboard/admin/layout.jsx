export const metadata = {
  title: "Admin Dashboard - Medisynix",
  description: "Manage Medisynix platform and users",
};

export default function AdminDashboardLayout({ children }) {
  return children;
}
